define(["base/trs/errorHandler"], function(ErrorHandler) {
    describe("example.hello", function() {
        it("should return 'hello jim'", function() {
            ErrorHandler.init();
           // expect(example.hello("jim")).toBe("hello jim");
        });
    });
});